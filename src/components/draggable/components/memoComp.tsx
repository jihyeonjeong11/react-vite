import React from "react";

import Window from "./Window";
import { Rnd } from "react-rnd";

const MemoComp = ({id}) => {
    return (
        <Rnd>
            <Window>
                <textarea />
                dddsdfasdfsf
            </Window>
        </Rnd>
    );
};

export default MemoComp;
